# Copy to .env and fill in. NEVER commit .env. (Names only here — no real secrets.)
# See ../docs/SECURITY.md §6 (secrets management).

# PostgreSQL
POSTGRES_USER=brook
POSTGRES_PASSWORD=change-me
POSTGRES_DB=brook

# MinIO (object storage)
MINIO_ROOT_USER=brook
MINIO_ROOT_PASSWORD=change-me

# api
JWT_SIGNING_KEY=change-me-long-random
JANUS_ADMIN_SECRET=change-me

# (Phase 4) coturn
# TURN_SECRET=change-me
