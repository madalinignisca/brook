# secrets
.env
**/.env

# rust
target/
**/target/

# python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
*.db

# review artifacts (transient)
.rev-*.out

# node (if mediasoup path is taken later)
node_modules/

# build / os
dist/
build/
.DS_Store

# data volumes (if bind-mounted locally)
pgdata/
miniodata/
caddydata/

# Editor/LSP scratch (qmlls writes a machine-specific absolute buildDir)
.qmlls.ini
# Apple bindings: build outputs and local test credentials
bindings/apple/build/
bindings/apple/.itest.env
bindings/apple/swift/BrookCore/BrookCoreFFI.xcframework/
bindings/apple/swift/BrookCore/Sources/BrookCoreGenerated/
bindings/apple/swift/BrookCore/.build/
bindings/apple/swift/BrookCore/.swiftpm/

# macOS client: generated project and build output
clients/macos/Brook.xcodeproj/
clients/macos/build/
clients/macos/Brook/Info.plist
clients/macos/Brook/Brook.entitlements
